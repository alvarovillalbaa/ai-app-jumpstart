# Delivery status

Updated 2026-09-24. The original goal remains active. `IMPLEMENTATION.md` remains the detailed target; this is not a production-readiness certificate.

Implemented: shared owner-scoped record service with SQLite/PostgreSQL/Supabase/Convex adapters; bearer keys and verified Supabase identities, scopes, input bounds, pagination and optimistic concurrency; REST, CLI, MCP tools/resources and a records screen; optional Supabase signup/login/confirmation/recovery/password-update/logout and private account records; portable durable session reservations/bindings, revocation tombstones and replay receipts; signed creation coordination, a session authorizer and runtime ownership receipt with lost-response recovery; private conversation history with titles, cursor pagination, rename/archive/restore and legacy metadata upgrades across all four stores; a shared metadata service exposed through REST, account-only MCP tools/resources and CLI commands; owner-scoped [conversation stream projections](conversation-projections.md) with bounded replay recovery across all four stores; a separate, opt-in [structured output reference](structured-output.md) with a fixed server-chosen schema, editable generated fields and versioned saved-record drafts; an [approval-gated private artifact reference](approved-artifacts.md) with durable same-call idempotency, owner-only reads, text download and replay-resistant owner deletion; an owner-only [AI usage view](usage-budgets.md) across browser, REST, CLI and MCP; reusable backend/DOM/provider contract tests, deterministic and live-model evals, production browser tests and CI; migration runner, Docker/Compose, environment example, health endpoints and operations documentation. Provider harnesses run real isolated PostgreSQL, PostgREST, Convex and Supabase Auth services. Root app checks preserve the legacy `my-agent/` scaffold separately. Production model selection is unchanged.

Still required:

1. Verify hosted PostgreSQL/Supabase/Convex configurations and operational behavior; local service contracts now pass.
2. Review actual provider prices and enforceable per-call token bounds, then reconcile disputed outstanding starts or failed settlements. Enabled chat now requires a source-attributed cost basis in both Next and Eve; a shared integer quote check rejects mismatched models and underfunded reservations, and `check:budget-policy` gives operators a secret-free preflight. The declared token counts are not runtime-enforced caps, and no vendor price is bundled. Authenticated browser/API broker wiring, pending-only cancellation with zero-cost settlement (including budget-only fencing), a read-only operator-wide outstanding-start inventory, an append-only correction path for already-settled costs, and an owner-only usage interface across browser/API/CLI/MCP are implemented; [account chat](account-chat.md) is opt-in and disabled by default. The production hook admits and settles account-owned turns and compaction, with durable model-attempt caps; see [usage budgets](usage-budgets.md). Creation recovery, owner checks and runtime budget denials pass through a real Eve runtime; see [agent session access](agent-session-access.md). Confirmed-dead PostgreSQL Workflow workers now have a manual, prefix-checked queue-unlock command; it does not reconcile ambiguous starts automatically. Enabled account sessions have no local-development/OIDC fallback; Eve attachments and default tools remain disabled while authored tools are available. Verify hosted identity/SMTP configuration separately.
3. Build exact source-order/canonical transcript and materialized run views, background projection recovery with checkpoints, a dedicated typed-draft API/edit history if required, artifact versioning and the remaining private-upload path, plus the acceptance cases in `IMPLEMENTATION.md`. The structured-result screen now attempts one bounded replay for a missing or overdue result; this does not repair unattended conversations. Structured results can be saved as versioned owner-scoped records and reopened for editing; the generic records API can also edit their raw JSON, so the editor validates on load. Artifact text download and owner deletion with replay-resistant tombstones are implemented; deletion covers the artifact row, not copied conversation/events or backups. A separate [private upload foundation](uploads.md) validates bytes and owner-derived keys, has private local and Supabase Storage object adapters, and reserves owner-scoped metadata/quota atomically across SQLite, PostgreSQL, Supabase and Convex before putting bytes in quarantine. The opt-in, 4 MiB browser/HTTP/CLI upload path and MCP metadata/delete tools are owner-scoped; no surface returns quarantined bytes. A self-hosted ClamAV Unix-socket scanner can reject infected or unavailable scans before object storage; the configured path fails closed, while clean files still remain quarantined. Protocol tests use a daemon fixture, and a live clean/EICAR preflight passed against ClamAV 1.5.4 with signature database 28133 in a disposable container; see [upload evidence](uploads.md). A real local Supabase Storage/API/catalog run passed, including anonymous denial, HTTP quarantine and a one-writer quota race. The general browser test covers local private-storage rejection, quota/status display, reload, owner isolation and deletion; the real-Auth browser test covers two signed-in accounts, cross-owner REST/MCP denial, owner-only metadata and account switching. Managed-host scanner deployment, quarantine release policy, cleanup worker and authorized downloads remain open; hosted Storage is unverified. Hosted in-flight recovery and operational replay remain unverified. Selected stream events are captured and readable through REST/CLI/MCP, but cannot identify the winning model-history attempt or certify complete capture; see [conversation stream projections](conversation-projections.md).
4. Complete hosted/cloud acceptance and a credentialed production-model turn. The production image now copies pruned runtime dependencies and required compiled/source files, excluding Turbopack's build cache; it builds from the full source and passes both the record-data contract and deterministic owned-turn browser suite in a container. A build marker records the compiled Workflow world, and the production supervisor refuses a cloud runtime expecting PostgreSQL when given a default-world artifact. The supplied AWS/Azure/GCP manifests and PostgreSQL Compose overlay require this check. The cloud manifests now include a packaged Caddy ingress on port 8080 that routes Eve/Workflow directly, with provider acceptance still pending. The combined application/Workflow PostgreSQL Compose stack passes local migration, readiness and app-replacement checks on both ARM64 and emulated amd64 builds. Cloud control-plane validation/deployed acceptance, automatic in-flight checkpoint recovery and concurrent rollout remain pending. PostgreSQL Workflow completed-session replay/continuation and a manual fail-closed in-flight restart after confirmed worker death pass locally. Amplify's documented Next.js/streaming support does not cover the pinned app; its compatibility requirement remains unmet after the 2026-09-24 AWS documentation check.
5. Complete app shell/accessibility/theming, configured optional integrations, and broader AI failure/denial/cancellation evals. A shared responsive navigation shell now covers workspace routes, with feature-gated links, keyboard skip navigation, distinct page titles, a 404 route and retryable error boundaries. System/light/dark theme choice persists in the browser and responds to system preference changes. Automated axe-core checks now cover records, account entry, enabled workspace screens, completed chat, approval and structured-result states; the chat layout's missing level-one heading was fixed. Manual screen-reader review, remaining shell states and cross-device preference sync still need work. Nine deterministic Eve evals now cover arithmetic, tool error, unavailable live data, quoted instructions with the artifact tool available, anonymous artifact denial, in-flight cancellation, malformed tool input and scripted model 429/503 failures; their fixture model does not establish production-model quality.
6. Finalize release policy, rehearse the eventual release revision after the remaining changes, and validate deployed runtime behavior. The original template code now has an MIT license, with upstream terms retained for copied components; a pre-release security policy and contribution guide are present. Private vulnerability reporting is enabled for the source repository. Next-served HTML routes now have per-request nonce-based script/style-element CSP, with inline style attributes allowed for the current components; APIs retain baseline directives. Eve/edge headers and changed UI integrations still need separate review. CI's external actions are pinned to reviewed Node 24 release SHAs and its disposable PostgreSQL job previews migrations before and after apply. All seven jobs passed on the public draft PR's prior `22b7c2b` revision; the PR checks show newer revisions separately. A clean-clone install, checks, build, startup, readiness and deterministic AI evals passed at `b835b65`. Neither is a hosted-release proof.

For private uploads, owner-initiated deletion now retries rows already in `deleting` and can atomically claim a `pending` row older than 24 hours before removing its object. This repairs a crashed reservation on demand while preserving quota until cleanup succeeds; unattended cleanup and release/download policy remain open.

## Verification

| Check | Result |
| --- | --- |
| `npm run check` | Passed: route type generation, TypeScript, lint with 0 errors/9 existing warnings, and 199 root tests |
| Fresh-clone SQLite/API-key rehearsal | On 2026-09-24, a shallow clone of draft-PR head `b01b1c6` installed with `npm ci` under Node 24.15.0, passed `npm run check` and `npm run build:local`, and started both production services from `.env.example` plus two generated owner keys. Web/data/Eve health and `/records` returned 200; the documented seed created two records and a rerun created none; the browser displayed both after reconnect and reload. `smoke:hosted` passed locally across two owners, REST, CLI and MCP, then removed its temporary record. |
| Fresh-clone local Supabase rehearsal | A separate shallow clone of PR head `53a02ad` installed under Node 24.15.0 and started a new local Supabase project in a Docker profile with no preexisting Supabase containers or volumes. All 13 repository migrations applied after a dry run and left zero pending; `db:types:check` matched the migrated schema. The documented `npm run dev` path served the app and Eve. Two disposable browser users signed up, received local confirmation emails and reached `/account`; Alice created a private record and reopened it after reload, while Bob's verified bearer read returned 404 and his list/browser omitted it. Two-account `smoke:hosted` passed web/Eve health, REST, CLI and MCP and removed its temporary record; `seed:records` created two fixtures and created none on rerun. This is local acceptance, not hosted Supabase, SMTP or cloud deployment proof. |
| Cost-basis validation | `npm run check:budget-policy` passed with a synthetic attributed policy; focused tests reject missing review, underquotes, mismatched models, unsafe source URLs and overflow. Current vendor prices/token bounds and a paid provider turn remain unverified |
| `npm run test:ai` | Passed: 9 deterministic Eve runtime evals, 27/27 gates. Four shared cases cover arithmetic, division-by-zero reporting, unavailable live data and quoted instructions with the artifact tool available; five fixture-only cases verify anonymous artifact denial, cancellation, malformed typed tool input and failed turns after scripted model 429/503 errors. A credentialed production-model run remains pending |
| Production build | Passed: Eve compilation plus Next production build, including request-time account/chat/history/structured/artifacts/usage pages, broker/status/history/cancel-start/metadata/projection/recovery/usage endpoints, artifact read/download/delete routes, records/API/MCP/health and session refresh proxy |
| `npm run build:eve` after budget-hook integration | Passed: production ownership/budget hook and shared adapters compiled; production model unchanged |
| `npm run test:session-runtime` | Passed: fresh temporary app/queue namespace; creation recovery, owned follow-up, manual compaction settlement, daily-budget denial, model-call cap on a tool loop, queued forged approval with no new call, owner isolation, revoked startup with no model call, and captured failed-run/tool events |
| `npm run test:workflow-postgres` | Passed: workflow bootstrap twice, compiled Eve worker with compatible pinned package, ownership/budget runtime contract, SIGKILL replacement with local workflow files removed, replay of completed history and owned follow-up. A second SIGKILL inside a model call left one locked Graphile job; the recovery CLI listed it and refused unconfirmed unlock, then unlocked the confirmed-dead worker. Replay did not make a second model call, reached a failed turn boundary and settled the unknown cost conservatively. Stale responses cannot create extra allowance after quota exhaustion |
| Workflow build and Compose configuration | Passed: production Eve builds with default and PostgreSQL worlds; default artifact restored afterward; merged application/workflow PostgreSQL Compose configuration validates |
| Workflow build marker and startup guard | Focused tests pass for matching default/PostgreSQL artifacts, wrong-world expectations, missing/invalid markers and missing PostgreSQL URL. A default-world `build:local` wrote the marker; a real supervisor invocation refused `WORKFLOW_EXPECTED_PROVIDER=postgres` before serving. The cloud manifests now require the PostgreSQL world. This guard checks artifact selection, not database connectivity or hosted workflow replay |
| `npm run test:workflow-compose` | Passed on a fresh linux/amd64 PostgreSQL Workflow image before the later structured-result recovery UI edit, and on an earlier ARM64 image: both PostgreSQL 17 databases healthy, all ten application migrations matched the source list, the artifact retention column and deletion function existed, Workflow migration succeeded with 13 tables, application and Eve health were ready, and an API record survived app-container recreation. Uses disposable Compose project, databases and volumes; no model turn or cloud ingress was exercised |
| Co-located readiness | The production supervisor now marks Next's `/api/health/ready` unavailable if the local Eve health route fails, while split-service/Vercel startup retains the data check. Four focused readiness tests passed; a fresh PostgreSQL Workflow container build and Compose run verified `data: ok` and `agent: ok` before and after app replacement. The probe checks HTTP health, not a model turn or a Workflow database operation |
| Split-service streaming ingress | A local Next 16 production build with `EVE_NEXT_PRODUCTION_ORIGIN` compiled the private-prefix Eve and Workflow rewrites. Through a live Caddy proxy, a direct Next rewrite combined two delayed SSE events into one chunk after completion, so that route is unsuitable for live token delivery. The new [split ingress](../deploy/split-app.Caddyfile) routes public `/eve/*` and `/.well-known/workflow/*` directly to Eve and other paths to Next. `npm run test:split-ingress` passed with co-located Docker mock servers and a proxy sharing their network namespace: app/API routing, loopback-only Eve health, Workflow body/query forwarding and separate SSE chunks before the upstream ended. A separate manual check passed with the real production Next server behind the same ingress and a mock Eve upstream: page/API, health, callback and live SSE all reached the correct service. The opt-in `compose.streaming.yaml` overlay validated with SQLite and PostgreSQL merges; a fresh SQLite image/stack passed real Next/Eve health, app readiness and authenticated record CRUD through Caddy, with only ingress port 3000 published. Its disposable containers and volumes were removed. A real deployed owned turn and provider load-balancer behavior are still unverified. |
| Linux amd64 PostgreSQL Workflow image | Passed a full Buildx `--platform linux/amd64 --load` build before the later structured-result recovery UI edit; Docker inspected the image as `linux/amd64`. The exact image passed the ten-migration Compose contract under emulation, including the artifact retention schema, 13 Workflow tables, app/Eve health and record persistence after app replacement. A 3 GiB Colima VM exhausted memory during an earlier build; the isolated profile succeeded at 6 GiB. No registry push or cloud deployment was performed |
| Cloud deployment definitions | AWS ECS/Fargate, Azure Container Apps and Cloud Run JSON syntax checked. Each routes public ingress to a packaged Caddy container on port 8080, with app and Eve reached over same-task/replica localhost; combined readiness includes Eve. The packaged proxy passes local path, callback and delayed-SSE tests. Provider control-plane, external load-balancer streaming and hosted acceptance have not run |
| `npm run test:e2e` | Passed: 7 Chromium/HTTP tests on the production app, including per-request CSP nonce rotation with no browser policy violations during hydration/theme changes, automated axe scans of disconnected/connected/dark records, mobile navigation and 404; a real 404 and recovery link, keyboard/mobile workspace navigation with disabled-feature links hidden, distinct page titles, persisted light/dark selection and live system-preference changes, create/reload/delete, health/access control, and closed Eve session creation/streams/control routes |
| Next response security headers | Production build and browser HTTP contract passed: page/API `nosniff`, `no-referrer`, frame denial; HTML page scripts and style elements use request nonces, while style attributes remain allowed. The real Supabase Auth browser contract passed all 5 cases after the header change. Eve and hosted edge-header behavior remain unverified |
| `npm run test:auth` | Passed: 6 Chromium contracts using real Supabase Auth/PostgreSQL/SMTP; signup, PKCE and token-hash confirmation, recovery, logout, reload persistence, two-account private uploads with cross-user REST/MCP isolation and deletion, revoked-session and forged-token rejection, plus axe scans of login, signup, recovery, account, uploads and password-update states |
| `npm run test:chat` | Passed all 11 browser/HTTP contracts locally against real Supabase Auth, production Next and compiled Eve. The hosted-smoke case reuses the post-deployment data check with two disposable signed-in users, then verifies one owned deterministic agent turn, durable binding, completed stream/projections and foreign-user denial. The structured flow withholds a result projection until one authenticated replay succeeds, then covers unsaved reload, saved reload, revision update, REST/CLI/MCP access, account-record reopening and cross-owner denial. Other paths cover shared-menu navigation, owned creation/replay/follow-up, approved artifact write/denial/download/deletion, owner-only usage, stream projections, history edits, cancellation, revoked credentials and rejected-start errors. Axe scans cover empty/completed chat, approval, editable structured fields, conversations, artifacts and usage |
| `npm run test:chat:container` | All 11 browser/HTTP contracts passed locally against the existing default-world Linux image, including the hosted-account mode, accessibility, automatic result-projection replay, saved reload/revision/account-record reopening and artifact download/deletion. The prior ten-case suite passed in CI against a fresh image at `0719d45`; the earlier nine contracts also passed against a pre-recovery-UI linux/amd64 PostgreSQL Workflow image under local emulation. The deterministic Eve fixture replaces each image's Eve output during this test; the image's normal Eve output passed the container runtime contract separately. No paid production-model call was made |
| SSR client isolation | Passed: additional regression test ensures browser-client instances are not cached across server renders; targeted lint passed |
| CLI command startup | `npm run app -- help` passed; record CRUD through HTTP handlers and conversation metadata through production HTTP with verified account tokens |
| `npm run test:postgres` | Passed: 37 shared provider tests on real isolated PostgreSQL, including atomic upload-catalog quota and owner isolation; the migration harness applies the source migrations twice |
| SQL migration dry run | Earlier ten-file check passed against a fresh disposable PostgreSQL database with no ledger/schema creation; after applying twice, zero pending files remained. The current provider harnesses applied the added upload migrations twice. Both dry-run and apply reject an applied ledger entry absent from the checkout. `npm run db:migrate -- --dry-run` is read-only and does not execute migration SQL |
| `npm run test:supabase` | Passed: 39 tests on real PostgreSQL/PostgREST, including upload-catalog quota and direct table/RPC denial for anonymous/authenticated roles |
| `npm run test:convex` | Passed: 36 shared provider tests, including upload-catalog quota and owner isolation, on a real local backend |
| `npm run test:upload-storage:live` | Passed: 4 tests against real local Supabase Storage with a private bucket, anonymous read denial, object isolation, HTTP quarantine and a composite catalog/Storage one-writer quota race; no hosted Storage test |
| Creation signing, coordinator and session policy | Passed: body binding, replay, timestamps, rotation, concurrent creation, candidate withholding, ambiguous-start persistence, ownership and revocation; enabled public API/channel wiring uses the same implementation |
| Usage reservations and budgeted creation | Passed: all four provider contracts cover concurrent admission, daily/active/rate limits, owner-scoped reservation lookup, bounded oldest-first operator inventory, idempotent settlement, append-only compare-and-swap correction of settled costs, unknown costs, overages, midnight and cancellation/binding races; creation tests deny dispatch on quota/provider errors and fence budget-only starts before delayed dispatch. SQLite CLI contracts verify both the read-only inventory and correction preview/apply/retry/history |
| Runtime budget lifecycle | Passed: known-cost settlement, missing retry usage, manual/automatic compaction accounting, approval continuations without `turn.started`, durable call caps, policy-cap increases, model allowlist checks and partial/unrepresentable overages retained for reconciliation |
| ANSI dependency compatibility and audit | Passed: 2 rendering tests; patched scoped override; audit reports 0 vulnerabilities |
| Linux ARM64 production image | A fresh current-source default-world image passed the full container runtime contract: non-root process, Eve/data health, owner isolation, restart persistence and shared REST/MCP/CLI access. That exact image then passed all nine container chat browser checks, including automatic result recovery. A separate ten-migration ARM64 PostgreSQL Workflow image passed the Compose contract above before the later UI changes. Production dependency pruning, selective copy and excluding the build-only Turbopack cache reduced an earlier image's Docker-reported size from 2.64 GB to 1.9 GB. The isolated Colima VM now has 40 GiB of disk for repeated builds |
| Container PostgreSQL migration | The fresh pre-recovery-UI Workflow Compose run applied all ten application migrations, including artifact retention and correction audit, and verified the retention column/deletion function and outstanding-budget index in PostgreSQL 17. Isolated PostgreSQL/PostgREST provider harnesses also ran all ten migrations twice to confirm safe reruns. The migration command uses Node 24 directly, so the runtime image does not need the development-only `tsx` package |
| PostgreSQL Compose startup | Passed with the pruned image and disposable local configuration: database healthy, migration service exited successfully, application healthy, and `/api/health/ready` reported `data: ok`. Containers, volumes and temporary configuration were removed afterward |
| Production startup changes | Scoped lint and syntax checks passed; container contract uses final supervisor and direct Node entrypoint |
| Hosted data and agent smoke | A reusable `npm run smoke:hosted` check exercises one temporary record across REST, CLI and MCP plus health and owner isolation, with an optional Vercel protection-bypass header. Its `--accounts` mode additionally requires two verified Supabase users and their user-only budget views without a model call. Explicit `--agent` adds one owned model turn, completed Eve stream and application projections, with foreign-user denial. API-key mode passed against the local production build and a container image; `--agent` passed against the local production app with real disposable Supabase Auth and a deterministic compiled Eve model. No hosted origin or paid model turn has been tested |
| `git diff --check` | Passed |
| CI workflow static check | Passed YAML parse: seven jobs, 16 SHA-pinned external action steps, repository read-only token permission, and disposable PostgreSQL migration previews before/after apply. The Vercel job builds the generated Next/Eve service graph without deployment credentials. The container job installs Chromium and runs both the image runtime contract and real-Auth chat browser contract against that image |
| Remote GitHub Actions | [Draft PR #1 Quality run](https://github.com/alvarovillalbaa/ai-app-jumpstart/actions/runs/35989366062) passed all six jobs at `b835b65`: contracts (including AI and PostgreSQL Workflow runtime), browser/Auth/chat with axe scans, PostgreSQL migrations/integration, provider portability, image runtime/container chat with the tenth accessibility contract, and PostgreSQL Workflow Compose. The initial run exposed a completed-job lock race and missing host build manifest; both were fixed before this passing run |
| Clean local Git-clone rehearsal | Cloned local feature commit `b835b65` with `git clone --no-local`: no dependencies, caches, generated output, `.env.local` or Supabase CLI temp marker were present. Node 24.15.0 `npm ci` reported 0 vulnerabilities; `npm run check` passed 153 tests with 0 lint errors/9 existing warnings; `npm run build:local` passed. The supervised production startup returned HTTP 200 for `/records`, and both web and Eve readiness reported ready with data and agent checks OK. `npm run test:ai` passed 6 deterministic evals and 17 gates from the clone. Vitest concurrency remains capped at four after its CLI-heavy correction test twice exceeded the default five-second timeout under the prior 29-way parallel run. Rehearse the final release revision again after further changes; this local clone does not prove hosted acceptance |

The local Supabase onboarding path now has a pinned CLI and project config with repository-owned SQL. On 2026-09-24, a fresh isolated Colima profile started PostgreSQL, Auth, PostgREST, gateway and local mail; the migration runner previewed and applied all ten files; a disposable confirmed user signed in, created and listed a private record through the live Next API. A separate `git clone --no-local` of `c541330` had no dependencies, secrets or build output; `npm ci`, migration preview, `/signup` and readiness passed, and its tracked tree remained clean. The clone reused the running local Supabase project, so this does not prove a separate hosted project or a clean database per clone. The `next dev` server was started after `next typegen` completed; running them concurrently caused transient 404s until a dev restart.

The public Supabase schema now has committed CLI-generated types used by the record, conversation/artifact and budget adapters. `db:types` and `db:types:check` refuse a local database with pending repository migrations; the PostgreSQL CI job checks the generated file against its migrated disposable loopback database. A plain PostgreSQL 17 comparison matched the local Supabase output byte-for-byte. The provider-neutral `seed:records` command uses the shared API and a fixed two-record fixture, preflights existing owner records, refuses content collisions and requires explicit opt-in for a remote URL. On 2026-09-24 the local Supabase-backed app returned two creations, then zero creations/two existing records on a sequential rerun; the credential and ignored environment file were removed. A focused CLI test covers reruns, owner isolation, content collisions and remote-write refusal. Typecheck, lint and 157 root tests passed after the seed change; the 34-test real PostgREST contract passed after generated types were applied across the Supabase stores. The PR's current CI run is the authoritative remote result for the drift step.

The managed Vercel build path now has a credential-free local contract: `npm run test:vercel-build` compiles Next with `VERCEL=1`, executes the generated Eve service build command, and checks the public `/eve/v1/` route, Workflow callback, Node 24 function metadata and response streaming. It passed locally without `.env.local` or a linked Vercel project. This tests generated build topology, not Vercel control-plane deployment, hosted Supabase credentials, callback reachability or a real agent turn. CI runs it as a seventh quality job.

The offline `npm run check:managed-config` command now validates the intended Vercel + Supabase runtime shape with the same application, Auth and optional chat parsers used by requests. It requires explicit HTTPS origins, backend/public key separation and the default Workflow world; `-- --require-chat` refuses a records-only release. `npm run check` passed 166 root tests with 0 lint errors and 9 existing warnings, including focused tests for both modes and unsafe/incomplete settings. This does not contact Vercel or Supabase, verify real keys or reviewed model prices, apply migrations or certify a hosted turn. The current checkout has no linked Vercel project, and local Vercel/Supabase CLIs were logged out on 2026-09-24, so no hosted project was changed.

Production application readiness now requires an explicit browser-facing `APP_ORIGIN`; shared validation accepts only HTTPS origins or loopback HTTP for the app, Supabase backend/Auth and Convex endpoints. This prevents the backend Supabase secret from being sent to a remote plaintext URL and makes a missing deployment origin visible before serving data requests. The container contract sends a browser-style Origin header to its published port. On 2026-09-24, `npm run check` passed 160 root tests, `npm run test:providers` passed 33 PostgreSQL, 34 Supabase/PostgREST and 32 Convex tests, the production build and six public browser tests passed, and a compiled local production server returned readiness 503 without `APP_ORIGIN` and 200 with the matching loopback origin. The first CI run passed five jobs, including the updated container and authenticated chat contracts; its isolated production Eve fixture lacked `APP_ORIGIN` and failed the contracts job. That fixture now sets its actual loopback origin, and both local runtime modes pass. [The corrected CI run at `0df572d`](https://github.com/alvarovillalbaa/ai-app-jumpstart/actions/runs/36002069923) passed all six jobs.

The CLI has a private, atomic, no-clobber [application-visible export](data-access.md#export-visible-application-data) over the shared authenticated API. Its v5 application mode includes a selected Supabase Auth profile snapshot, all owner-scoped records, archived and active conversation metadata, selected event projections, saved artifacts, active private-upload metadata and upload quota usage, historical budget reservations and owner-visible correction cost changes, plus the AI usage snapshot. The profile and budget histories are also available through authenticated REST, CLI and account-only MCP reads. A records-only mode works with record read credentials when account chat is disabled. The NDJSON manifest explicitly excludes private upload bytes, deleted upload/artifact tombstones, remaining Auth data and credentials, Eve session/model history and checkpoints, budget model-attempt IDs, operator correction notes/evidence, historical daily aggregates and backups; paged live reads are not a consistent database snapshot. The shared ledger contract passed on SQLite and disposable PostgreSQL, Supabase/PostgREST and Convex backends, including owner isolation, stable pagination and redacted correction costs. The focused export tests passed multi-page budget sections and cleanup after a later-page failure; the real Auth/chat browser suite passed all 12 tests, including a nonempty correction through REST/CLI/MCP/export with foreign-user denial. `npm run check` passed 202 root tests, typecheck and lint with 9 existing warnings. Full account export and deletion remain incomplete.

The test suite also covers legacy SQLite/Convex metadata upgrades, history pagination/filter races, projection deduplication/late-arrival cursors/partial recovery, artifact same-call replay/input conflicts, deletion tombstones and owner isolation, shared REST/MCP/CLI ownership and revision behavior, record-key denial for matching owner strings, stale edits, title validation, records, ownership, replay receipts and budget persistence after closing/reopening SQLite and ignores stale authorization responses after account switching. Earlier ownership, budget, history, projection and artifact migrations were generated with Supabase CLI 2.117.0; the correction and artifact retention migrations were added directly to the canonical `migrations/` directory, whose ordered SQL files the runner applies. Budget, projection, artifact and correction RPCs use invoker security and backend-only grants. The implementation is published on a feature branch in draft PR #1; it has not been merged or deployed.

Hosted provider contracts, cloud acceptance and paid model checks have not run. Local and CI provider evidence does not certify hosted networking, secrets, backups or deployment policies. The container chat suite substitutes a deterministic compiled Eve fixture for the image's model-bearing server output; it proves the container path through the production channel/hooks, not a credentialed call from the unchanged production model. The multi-stage build still needs substantial temporary disk space in a small Docker VM. The auth build is independently verified: it is compiled without a hosted Auth configuration, then run with the disposable provider configuration, and its protected pages render per request. The `ansi-to-react` dependency override uses compatible patched `linkify-it` 5.0.2, with regression coverage for its rendering and fuzzy-link API.
